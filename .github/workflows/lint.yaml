name: lint-colors

on:
  push:
    branches:["*"]
  pull_request:
    branches["*"]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      -name: Checkout repository
       uses: actions/checkout@v4
